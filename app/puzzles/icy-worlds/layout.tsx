export const metadata = {
  title: 'Icy Worlds — SSIP Universium',
  description: 'Navigate pressure and temperature to discover the exotic ice phases hidden inside Neptune.',
}

export default function IcyWorldsLayout({ children }: { children: React.ReactNode }) {
  return children
}
